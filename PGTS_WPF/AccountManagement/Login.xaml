<Window x:Class="PGTS_WPF.AccountManagement.Login"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PGTS_WPF"
        mc:Ignorable="d"
        Title="Login" Height="400" Width="600"
        Background="#B3E5FC"
        WindowStartupLocation="CenterScreen">
    <Window.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="FontFamily" Value="MV Boli"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="Foreground" Value="#FFFFF0"/>
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="TextAlignment" Value="Right"/>
            <Setter Property="BorderBrush" Value="#C1E1C1" />
            <Setter Property="Background" Value="#FAF3E0"/>
            <Style.Resources>
                <Style TargetType="Border">
                    <Setter Property="CornerRadius" Value="5"/>
                </Style>
            </Style.Resources>
        </Style>
        <Style TargetType="Button">
            <Setter Property="FontFamily" Value="MV Boli"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="Foreground" Value="#AEC6CF"/>
            <Style.Resources>
                <Style TargetType="Border">
                    <Setter Property="CornerRadius" Value="25"/>
                </Style>
            </Style.Resources>
        </Style>
    </Window.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="3*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="8*" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Grid.Column="1" Grid.Row="1">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="1*" />
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="1*" />
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="1*"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="1">
                    <TextBlock Text="Email" Margin="0,15,0,0"/>
                    <TextBox x:Name="txtEmail" Margin="0,5,0,0" Height="20"/>

                    <TextBlock Text="Password" Margin="0,15,0,0"/>
                    <TextBox x:Name="txtPass" Margin="0,5,0,0" Height="20"/>
                </StackPanel>

                <StackPanel Grid.Row="3" VerticalAlignment="Bottom">
                    <Button Content="Login" Width="200" Height="40" FontSize="16" FontWeight="Light"
                        Background="#FFFFF0" BorderBrush="#C1E1C1" Cursor="Hand" Click="btnLogin_Click" />

                    <Button Content="Cancel" Width="200" Height="40" FontSize="16" FontWeight="Light"
                        Background="#EAEAEA" BorderBrush="#C1E1C1" Cursor="Hand" Margin="0,10,0,0" Click="btnCancel_Click"/>
                </StackPanel>
            </Grid>
        </Grid>
    </Grid>
</Window>
